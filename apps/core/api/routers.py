from rest_framework.routers import DefaultRouter

# from apps.core.api.views.general_views import TestAPIView


router = DefaultRouter()

# router.register(r'test', TestAPIView.as_view(), basename='test')

urlpatterns = router.urls
